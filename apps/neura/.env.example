# Solana devnet RPC endpoint.
#
# Optional. Falls back to https://api.devnet.solana.com, which is rate-limited
# hard enough that it is fine for local development and not for anything shared.
VITE_SOLANA_DEVNET_RPC_URL=
